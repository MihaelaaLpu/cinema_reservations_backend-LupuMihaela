spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cinema_db?serverTimezone=UTC
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # hibernate will automatically create the tables based on the code from entity package
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect